export CLICOLOR=true
if [ `uname` == "Darwin" ]; then
    alias vi=~/bin/mvim
    alias vim=~/bin/mvim
fi
alias view="vim -R"

export PS1="\h:\W \u\$ "
export EDITOR=vim

. ~/.git-completion.bash

[ -e ~/.workrc ] && . ~/.workrc
